# =============================================================================
# 本地開發機敏設定 (此檔案不提交到 Git)
# =============================================================================
# 使用方式:
#   1. 複製此檔案: cp application-secrets.properties.example application-secrets.properties
#   2. 編輯填入實際值
#   3. 啟動應用程式: ./gradlew bootRun
#
# 屬性名稱與 GCP Secret Manager 的 secret 名稱一致
# 本地: ${platform-xxx} 從此檔案讀取
# GCP:  ${sm@platform-xxx} 從 Secret Manager 讀取
# =============================================================================

# Google GenAI API Key (用於 Embedding)
# 取得方式: https://aistudio.google.com/app/apikey
platform-google-api-key=your-google-api-key-here

# ===== Mock OAuth2 Server（本地開發用） =====
# platform-oauth2-issuer-uri=http://localhost:8090/default
# platform-oauth2-client-id=test-client
# platform-oauth2-client-secret=test-secret

# 資料庫連線 (可選，若需覆蓋預設值)
# platform-db-url=jdbc:postgresql://localhost:5432/mydatabase
# platform-db-username=myuser
# platform-db-password=secret
